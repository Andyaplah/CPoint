//
// Created by LCX on 2020/8/25.
//

#include <stdio.h>

struct point* getStruct(struct point*);
void output(struct point);
void point(const struct point *p);

int main()
{

    return 0;
}

struct point* getStruct(struct point *p)
{

}